# PDF Metadata Core

Shared PdfBox/Jackson helpers that power the CLI tools in this directory. Place reusable parsing/writing code here so both `pdf-metadata-extractor-cli` and `pdf-metadata-writer-cli` can stay thin wrappers around the shared implementation.
